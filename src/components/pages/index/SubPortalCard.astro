---
// src/components/pages/index/SubPortalCard.astro
const { titulo, descripcion, imagen, enlace, textoBoton } = Astro.props;
---

<article class="bg-white rounded-xl shadow-md hover:shadow-xl transition-shadow duration-300 overflow-hidden flex flex-col">
  <img 
    src={imagen} 
    alt={titulo} 
    class="w-full h-48 sm:h-56 object-cover transition-transform duration-300 hover:scale-105"
  />

  <div class="p-6 flex flex-col flex-grow justify-between">
    <div>
      <h3 class="text-lg font-semibold text-gray-800 mb-2">{titulo}</h3>
      <p class="text-sm text-gray-600 leading-relaxed">{descripcion}</p>
    </div>
    <div class="mt-4">
      <a 
        href={enlace} 
        class="inline-block px-4 py-2 text-sm font-medium text-white bg-green-600 hover:bg-green-700 rounded-md transition-colors"
      >
        {textoBoton}
      </a>
    </div>
  </div>
</article>
